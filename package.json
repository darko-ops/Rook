{
  "name": "rook",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*",
    "sim"
  ],
  "scripts": {
    "test": "vitest run",
    "sim": "npm run -w sim start --"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
